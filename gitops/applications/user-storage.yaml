apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: user-storage
  namespace: openshift-gitops
spec:
  generators:
  - list:
      elements:
      - env: dev
      - env: int
      - env: prod
  template:
    metadata: 
      name: '{{env}}-user-storage'
      labels: 
        env: '{{env}}'
        type: app
        subtype: go
    spec:        
      destination:
        namespace: '{{env}}'
        server: "https://kubernetes.default.svc"
      source:
        path: user-storage
        repoURL: "http://gogs.gogs.svc.cluster.local:3000/gogs/workshop-argocd.git"
        targetRevision: develop
        helm:
          valueFiles:
            - '{{env}}.values.yaml'
      project: default
      syncPolicy:
        automated:
          prune: true
          selfHeal: false
        syncOptions:
          - CreateNamespace=true